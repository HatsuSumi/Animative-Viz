# 项目架构问题记录

本文档记录当前项目中已识别出的 10 个架构与工程层面的改进点，作为后续整理与重构的参考。

## 优先级说明

### P0

必须优先处理，已经影响：

1. 语义正确性
2. 接口稳定性
3. 核心数据一致性
4. 后续修改安全性

### P1

重要但不阻塞当前主线，适合本轮或下一轮整理：

1. 结构收口
2. 可维护性提升
3. 复杂度控制
4. 中期扩展能力

### P2

优化项或工程整洁项：

1. 不改也能继续开发
2. 但会持续积累技术债
3. 适合在主问题收口后处理


## 1. [P0] 路由层异常处理不够干净

问题位置：`backend/src/routes/data_routes.py`

当前几个接口的处理模式大致是：

1. 先手动 `raise HTTPException(...)`
2. 外层再统一 `except Exception`
3. 最后重新包装成 500

这会带来一个实际问题：

1. 本来明确抛出的 400 或 404
2. 最后也可能被外层吞掉
3. 重新包装成 500

这不是单纯的代码风格问题，而是语义污染。

路由层本来应该做到：

1. 业务已知错误保留原状态码
2. 未知异常再转为 500

现在这个结构让错误边界不够优雅，也会让前端更难准确区分错误类型。

## 2. [P1] `VoteTracker` 过于强大，已经开始变成“业务上帝类”

问题位置：`backend/src/vote_tracker.py`

虽然核心业务集中本身是优点，但它也有另一面。

当前 `VoteTracker` 同时负责：

1. 读 CSV
2. 清理列名
3. 识别赛季
4. 查赛季配置
5. 过滤轮次
6. 查淘汰角色
7. 截断票数
8. 算参与人数
9. 组装角色基础信息

它现在还没烂，但已经有“单类承载过多职责”的趋势。

继续加功能的话，这个文件会很快变沉。

更优雅的方向应该是按职责逐步收口，例如拆成：

1. 文件读取与校验
2. 轮次过滤策略
3. 淘汰规则处理
4. 统计计算

这不是说现在就必须重构，但它已经是明显的未来压力点。

## 3. [P1] “当前数据文件”是全局单例模型，扩展性一般

问题位置：`backend/src/services/vote_tracker_store.py`

当前设计是：

1. 全局 `_vote_tracker`
2. `.latest` 记录一个当前文件
3. 整个服务默认只服务一份当前数据

这对现在的单人本地工具场景没问题。

但从架构上讲，它属于很强的单上下文设计，限制包括：

1. 不能并行切多个赛季上下文
2. 用户状态与服务全局状态耦合
3. 后面要做多文件对比、历史回看、多个会话时会卡住

所以它不是错，而是当前场景适配，但不算优雅的通用模型。

## 4. [P1] 前端首页状态编排已经有点散

问题位置：`frontend/src/pages/HomePage.js`

当前页面里存在大量布尔状态，例如：

1. `showColumnExclusionModal`
2. `showConfirmationModal`
3. `showSpecialRoundsModal`
4. `showRecordModal`
5. `excludeWildcard`
6. `excludeRanking`
7. `shouldRecord`
8. `selectedColumns`

这在当前规模下还能工作，但已经不是最优雅的写法。

因为这个页面本质上不是简单表单，而是一个多步骤流程状态机：

1. 上传完成
2. 询问是否排除列
3. 选择列
4. 选择特殊轮次过滤
5. 选择是否录制
6. 跳转展示

这种流程更适合：

1. `useReducer`
2. 显式步骤枚举
3. 或小型状态机

现在这个页面不是不可维护，但逻辑已经开始横着长。

## 5. [P1] 图表数据整理有明显的性能型低优雅点

问题位置：`frontend/src/components/CumulativeVotesChart/chartData.js`

当前 `processChartData()` 里存在这种模式：

1. 先遍历轮次
2. 再遍历角色数据
3. 再在 `processedData` 里反复 `find`

这是标准的可跑方案，但不算优雅。

数据一大，复杂度就会上去。

同类问题在一些地方也出现过，例如：

1. 基于 `character` 反查数据
2. 反复排序
3. 反复 `find`

项目当前规模也许还能扛住，但从实现成熟度上看，它更像先跑通的实现，不是已经收敛的实现。

## 6. [P0] 前端仍有一部分 `id` 与展示字段双轨并存

这个问题在当前代码里已经能观察到。

虽然接口已经开始返回 `id`，但仍能看到这种状态：

1. 有时用 `id`
2. 有时用 `character`
3. 有时是 `id || character`

这说明主标识体系还没完全统一。

架构上最怕这种过渡态长期存在。

因为一旦长期并存，就会开始出现：

1. 有的逻辑按 `id`
2. 有的逻辑按中文名
3. 有的逻辑按 `character@ip`

这样会让 bug 定位和后续扩展越来越烦。

这个项目未来的优雅程度，很大程度取决于这块能不能彻底收口。

## 7. [P2] 错误处理体验比较原始

这点前后端都能看到。

后端部分问题：

1. 有些地方直接返回字符串型错误
2. 错误层级和语义不够统一

前端部分问题：

1. `FileUploader` 上传失败只显示“文件上传失败”
2. `ErrorBoundary` 出错直接返回 `null`
3. 图表页错误展示也比较基础

尤其 `ErrorBoundary`，当前等于页面挂了就什么都不显示。

这在开发期能接受，但从架构完成度看，不算优雅。

## 8. [P1] 配置完整性校验还不够体系化

当前我观察到，前端对 `currentSeasonConfig.rounds.find(...)` 找不到时，只是简单地：

1. `console.error('缺少轮次配置')`

这意味着：

1. 后端依赖赛季配置
2. 前端也依赖赛季配置
3. 但前后端配置对齐目前没有一套统一校验机制

这会导致一个问题：

1. 只要某个赛季新增时前后端有一边漏了
2. 你可能不是在启动时发现
3. 而是在运行到某个轮次时才发现

对这种强配置驱动项目来说，更优雅的做法应该是建立赛季配置完整性检查。

## 9. [P2] 工具脚本环境耦合很重

问题位置：`backend/scripts/`

当前几个脚本里能看到明显的绝对路径和外部盘符路径。

这说明它们更像作者个人离线工具，而不是项目内可复用工具。

这不影响主系统运行，但从整体工程一致性看，它们属于项目边角地带还没整理的部分。

后续如果要提升工程整洁度，这部分至少应该做到：

1. 去掉写死的本地绝对路径
2. 让脚本能基于项目相对路径运行
3. 明确哪些脚本是临时工具，哪些脚本是项目正式能力

## 10. [P2] 前端依赖里有一部分主链路未体现价值

问题位置：`frontend/package.json`

从当前主链路实际用到的依赖来看，明显在起核心作用的是：

1. React
2. Router
3. D3
4. Framer Motion
5. Axios

但 `package.json` 里还有一些依赖，在我阅读的主线文件里没有看到承担核心职责，例如：

1. `antd`
2. `chart.js`
3. `react-chartjs-2`
4. `recharts`

这不一定等于它们绝对没用，但至少说明一件事：

前端依赖层还有整理空间。

后续可以考虑：

1. 盘点真实使用情况
2. 删除历史遗留依赖
3. 降低无效依赖带来的维护成本

## API 接口设计专项补充

以下问题属于前面 10 个主问题在 API 设计层的展开与补充，不单独改写前面的主问题编号，但后续做接口治理时应一并考虑。

### A1. [P1] `votes-by-rounds` 同时支持 GET 和 POST，没有必要

问题位置：`backend/src/routes/data_routes.py`

当前这个接口后端同时注册了：

1. `GET /api/v1/votes-by-rounds`
2. `POST /api/v1/votes-by-rounds`

但前端实际走的是 POST。

这会带来几个问题：

1. 同一个业务语义有两种调用方式
2. 参数既可以从 query 来，也可以从 body 来
3. 路由内部还要写一层“优先用 request，否则用 query”的兼容逻辑

这不是优雅，而是接口语义变松了。

对这个接口来说，更合理的方向是：

1. 要么只保留 POST
2. 要么拆成明确的 GET 无过滤版本和 POST 过滤版本

现在这种双支持模式，价值不高。

### A2. [P0] 错误状态码语义不够稳定

这个问题是 API 层最明显的问题之一。

当前路由里存在这种结构：

1. 先主动 `raise HTTPException(status_code=400/404/500, ...)`
2. 外层再 `except Exception`
3. 再统一转成 500

这会导致：

1. 本来应该返回 400 的场景
2. 最终可能变成 500

对前端来说，这样的接口就不够可信，因为前端没法稳定依赖状态码去区分：

1. 用户操作问题
2. 数据未初始化
3. 服务内部异常

而 API 设计里，状态码稳定性本身就是设计质量的一部分。

### A3. [P0] 返回体结构没有统一错误协议

成功返回现在还算直观。

但错误返回更多是框架默认 `detail` 或直接字符串拼接，缺少统一格式。

更统一的 API 一般会明确成这种语义层：

```json
{
  "code": "DATA_NOT_INITIALIZED",
  "message": "请先上传数据文件"
}
```

当前接口更像是：

1. 某些地方是 `detail`
2. 某些地方是直接异常字符串
3. 某些地方 message 语气也不完全一致

这对小项目还能接受，但从 API 完整性来看，不算收口。

### A4. [P1] `current-season` 只返回一个裸字符串，不够稳

问题位置：`GET /api/v1/current-season`

当前接口直接返回赛季值，按实际代码大概率就是类似 `"2023"` 这样的裸字符串。

这当然能用，但不够稳的点在于：

1. 响应没有字段名
2. 扩展空间差
3. 前端拿到的是一个原始值，不是对象

更稳的接口形态应该更接近：

```json
{
  "season": "2023"
}
```

这样未来加字段时，不会破坏前端解析形态。

这不是大问题，但属于接口成熟度还没到位。

### A5. [P1] `vote-rounds` 也是直接返回裸数组，扩展性一般

问题位置：`GET /api/v1/vote-rounds`

当前接口直接返回轮次数组。

这和上面同理，当前虽然方便，但未来一旦想补充这些元信息：

1. 当前赛季
2. 轮次类型
3. 是否是 wildcard
4. 轮次排序信息

就必须直接改响应结构。

所以这种裸数组返回属于轻量实现，不是成熟接口设计。

### A6. [P1] `characters-info` 和 `votes-by-rounds` 有一点信息边界重叠感

当前角色相关信息分布大致是：

1. `votes-by-rounds` 返回角色票数主数据
2. `characters-info` 返回角色元数据
3. 图表页再自己组 `finalRanks`

这本身不一定错，但会带来一个 API 设计上的问题：

前端进入图表页时，实际上需要二次拼装两个接口的数据。

这意味着接口层没有完全对前端展示场景做“视图级聚合”。

当前方式偏通用，但对页面使用方来说不算最省心。

这块不一定必须调整，因为改过头又会让接口过胖，但它说明：

API 目前更像按后端内部资源切的，不是完全按前端页面模型切的。

### A7. [P1] 上传接口语义是“上传并切换当前上下文”，但名字没有完整表达出来

问题位置：`POST /api/v1/upload-data`

这个接口实际不是简单上传文件，而是会：

1. 校验文件
2. 落盘
3. 识别赛季
4. 初始化当前数据上下文
5. 写 `.latest`

所以它的真实语义更接近：

1. 导入当前数据源
2. 并将其设为当前活跃赛季数据

而不只是“上传文件”。

接口名本身没错，但从语义准确性看，它偏轻，这也是为什么当前 API 设计属于够用，不是已经很精致。

### A8. [P1] 接口还带着明显的单用户本地工具假设

当前所有接口默认共享一个全局当前数据上下文。

所以接口不需要显式传：

1. `dataset id`
2. `season context id`
3. `session id`

这在本地单人使用场景里很舒服。

但从 API 设计抽象度看，它意味着：

1. 接口不是上下文显式的
2. 服务状态和当前活跃数据强绑定
3. API 可复用性偏弱

所以它适合现在这个工具，但不算通用意义上的优雅 API。

## 总结

这 10 个问题里，有些属于当前就值得处理的结构问题，有些属于中长期收口项。

从优先级角度，可以初步理解为：

1. 需要尽快处理的：路由错误边界、标识体系收口、首页流程状态组织
2. 需要中期规划的：`VoteTracker` 收口、配置校验体系化、单例上下文模型演进
3. 需要工程清理的：工具脚本整理、依赖盘点、错误体验打磨
4. 需要 API 专项治理的：接口调用方式收口、错误协议统一、裸返回对象化、接口上下文语义明确

